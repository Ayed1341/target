#!/bin/sh
# Router Manager Gradle Wrapper - عايد عريبي (Ayed Oraybi)
# This script delegates to the system Gradle installed by gradle/actions/setup-gradle
set -e
GRADLE_OPTS="${GRADLE_OPTS:-} -Dfile.encoding=UTF-8 -Xmx2g"
exec gradle "$@"
